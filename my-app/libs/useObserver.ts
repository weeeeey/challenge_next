import { NextPage } from "next";
import { useEffect } from "react";

interface IObserver {
    target: number;
    onIntersect: () => void;
}

const useObserver = () => {
    return;
};
export default useObserver;
